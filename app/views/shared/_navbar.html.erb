<nav class="flex items-center justify-between px-3 py-2 text-white bg-slate-800">
  <div class="flex items-center gap-2">
  
    <h1 class="text-2xl font-bold">
      <%= link_to t('navbar.name') , root_path %>
    </h1>
    <section class="flex items-center gap-2">
      <ul class="flex gap-3">
        <li>
          <%= link_to t('navbar.products') , products_path%>
        </li>
        <li>
          <%= link_to t('navbar.about') , about_path%>
        </li>
        <li>
          <%= link_to t('navbar.privacy') , privacy_path%>
        </li>
      </ul>
      <form action="/search" data-turbo="false">
        <input type="search" name="q" value="<%= params[:q] %>" class="input input-bordered input-xs text-black">
        <button class="btn btn-xs">搜尋</button>
      </form>
    </section>
  </div>
  <div>
    
    <ul class="flex gap-3">
    <li class="relative items-center mr-4">
      <%= link_to cart_path do %>
        <%
=begin%>
 <%= image_tag "icon/cart.svg", class: "text-white"%> 
<%
=end%>
        <i class="fa-solid fa-cart-shopping fa-xl"></i>
        <% if current_cart.count > 0 %>            
          <span class="absolute badge badge-xs badge-secondary rounded -top-1 -right-2"><%= current_cart.count %></span>
          
        <% end %>
      <% end %>
    </li>
      <% if user_signed_in? %>
        <li><%= current_user.email %></li>
        
        <li>
          <%= link_to t('navbar.logout'), sessions_path, data: { turbo_method: "delete" } %>
        </li>
      <% else %> 
        <li>
          <%= link_to t('navbar.register') , new_users_path %>
        </li>
        <li>
          <%= link_to t('navbar.login') , sign_in_users_path %>
        </li>
      <% end %>
      
    </ul>
  </div>
  <%
=begin%>
 <%= current_cart.cart_items.count %> 
<%
=end%>
</nav>