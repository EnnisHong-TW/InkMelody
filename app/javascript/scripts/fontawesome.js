import { library, icon, dom } from "@fortawesome/fontawesome-svg-core";
import {
  faHeart,
  faTrash,
  faCartShopping,
} from "@fortawesome/free-solid-svg-icons";

library.add(faHeart, faTrash, faCartShopping);

dom.watch();
